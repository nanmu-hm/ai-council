# AI Council · 智能体交流与开发日志

> 用途：本文件是参与 `ai-council` 项目的不同智能体之间的共享工作区。
>
> 规则：每个智能体在修改代码前后，都应尽量记录自己的判断、发现、问题和建议；不要覆盖其他智能体已有内容。建议采用“追加日志”的方式交流。

---

## 2026-08-29 · GPT-5.6 Luna · 初始设计说明

### 1. 项目目标

本项目目标不是重新实现某一个 Web AI 平台，而是建立一个独立的 **Multi-Model AI Council（多模型协作与共识引擎）**。

核心思想：

```text
用户问题
   ↓
AI Council Orchestrator
   ↓
多个模型并发回答
   ↓
Streaming 汇聚
   ↓
Cross-feed / Peer Review
   ↓
Judge
   ↓
Consensus
   ↓
最终答案
```

底层模型来源应保持开放：

- WebModel 等 OpenAI-compatible Gateway
- 官方模型 API
- Ollama / LM Studio 等本地模型
- 后续可增加其他兼容接口

### 2. 为什么不直接 Fork WebModel

WebModel 的 Provider 层变化速度会很快，尤其是基于 Web 登录态的 Provider。

因此 AI Council 第一阶段只把 WebModel 当作一个外部 Gateway，通过 OpenAI-compatible API 调用，而不复制其 Provider 源码。

目标架构：

```text
AI Council
    │
    ├── WebModel adapter ──> WebModel ──> Web AI
    │
    ├── Ollama adapter ─────> Ollama ────> Local models
    │
    └── Future adapters ────> Official APIs / others
```

这样可以让 WebModel 独立更新，而 AI Council 保持稳定。

### 3. 第一阶段必须完成的能力

1. Provider 抽象接口
2. WebModel OpenAI-compatible adapter
3. Ollama adapter
4. 并发 Dispatcher
5. 统一 Streaming Event Protocol
6. 超时 / 重试 / 错误隔离
7. Provider Health Check
8. 基础 HTTP API
9. 自动化测试
10. Docker / 本地开发环境

第一阶段暂不追求：

- 复杂浏览器 UI
- 复杂 Evidence Research
- 自动化网页操作
- 十几个 Provider 的原生适配

先让最小闭环真正跑起来。

### 4. Streaming Event Protocol

不同模型的返回格式不能直接暴露给前端。

建议统一为：

```json
{
  "request_id": "...",
  "model": "...",
  "type": "delta",
  "text": "...",
  "timestamp": "..."
}
```

事件类型至少包括：

- `request.started`
- `model.started`
- `model.delta`
- `model.thinking`
- `model.completed`
- `model.error`
- `request.completed`

以后可以在不修改 UI 的情况下增加 Provider。

### 5. 并发策略

不能简单认为“模型数量 = 并发数量”。

应由 Scheduler 控制：

```text
Scheduler
  ├── global concurrency
  ├── per-provider concurrency
  ├── timeout
  ├── retry
  └── circuit breaker
```

单个 Provider 连续失败时，应停止自动重试一段时间，而不是继续产生请求。

### 6. Council 第二阶段

基础并发稳定以后，再加入：

```text
Round 1
各模型独立回答
    ↓
Round 2
模型互相审阅 / Cross-feed
    ↓
Judge
    ↓
Consensus
```

Judge 不应只是“让另一个模型总结答案”，而应产生结构化结果：

```json
{
  "consensus": [],
  "disagreements": [],
  "unsupported_claims": [],
  "strongest_answer": "",
  "confidence": 0.0,
  "needs_external_verification": false
}
```

### 7. 第三阶段：Evidence Verification

对于法律、政策、技术版本、事实性问题，不应因为多个模型答案一致就认定事实正确。

未来设计：

```text
Model Answers
     ↓
Claim Extraction
     ↓
Evidence Search
     ↓
Claim ↔ Evidence
     ↓
Judge
     ↓
Evidence-backed Consensus
```

### 8. 浏览器可视化模式

用户最初提出“所见即所得”的交互理念。

长期可以提供两种模式：

**后台模式**：

```text
Browser/session
    ↓
Provider/Web API
    ↓
Fast streaming
```

**观察模式**：

```text
真实浏览器页面
    ↓
用户可以看到登录态、页面和回复
```

第一阶段不应该因为追求视觉浏览器而拖慢核心引擎开发。

### 9. 给其他智能体的检查重点

如果你是另一台电脑上的智能体，请重点检查：

1. Provider abstraction 是否过度设计？
2. WebModel adapter 是否正确使用 OpenAI-compatible API，而没有把 WebModel Provider 逻辑复制进来？
3. async 并发是否存在任务泄漏？
4. SSE / streaming 是否能正确处理断开、空 chunk、异常和最终事件？
5. timeout / retry 是否可能导致重复请求？
6. circuit breaker 是否真正隔离单个 Provider？
7. 类型定义是否清晰？
8. 测试是否覆盖 partial failure？
9. Docker 环境是否可以在 Windows + WSL / Linux 上运行？
10. 是否存在把 Provider-specific 字段泄漏到核心层的问题？

### 10. 协作规则建议

请不要直接覆盖其他智能体的工作。

推荐：

```text
查看当前 main
   ↓
理解已有代码
   ↓
提出问题 / 建议
   ↓
修改自己的部分
   ↓
提交 commit
   ↓
在本文件追加日志
```

如果发现严重问题，请明确写：

```text
[BLOCKER]
问题：...
原因：...
建议：...
涉及文件：...
```

普通建议使用：

```text
[REVIEW]
...
```

已经解决的问题使用：

```text
[RESOLVED]
...
```

---

## 智能体回复区

### 2026-08-29 · 等待其他智能体检查

请后续智能体在这里追加自己的检查结果，不要删除上面的设计记录。

---
